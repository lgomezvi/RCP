services:
  frontend:
    build: 
      context: ./frontend
      dockerfile: ../Dockerfile.frontend
    ports:
      - "3000:3000"
  backend:
    build: 
      context: .
      dockerfile: ./Dockerfile.backend
    ports:
      - "8000:8000"
    volumes:
        - ./backend:/app/backend   # optional for live reload